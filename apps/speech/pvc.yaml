apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: speech-models
  namespace: speech
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
